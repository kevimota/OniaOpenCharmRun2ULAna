name: OniaOpenCharmRun2ULenv
channels:
  - conda-forge
dependencies:
  - python = 3.9
  - xrootd
  - coffea == 0.7.14
  - pip
  - root == 6.26.2
  - jupyterlab >= 3
  - ipywidgets
  - jupyterlab-system-monitor
  - pip:
    - pyyaml