name: OniaOpenCharmRun2ULenv
channels:
  - conda-forge
dependencies:
  - python = 3.9
  - xrootd
  - coffea == 0.7.7
  - pip
  - root
  - jupyterlab >= 3
  - ipywidgets
  - jupyterlab-system-monitor
  - pip:
    - pyyaml